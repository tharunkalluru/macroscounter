/** Shared input styling for the profile form (OnboardingFlow + SettingsPage) — one definition so the two never drift apart. */
export const TEXT_INPUT_CLASS =
  'min-h-touch rounded-card border border-slate-200 bg-white px-3.5 py-2.5 text-body text-slate-900 shadow-sm focus:border-brand-500 focus:outline-none focus:ring-2 focus:ring-brand-500/30 dark:border-slate-700 dark:bg-surface-dark-card dark:text-slate-100'
